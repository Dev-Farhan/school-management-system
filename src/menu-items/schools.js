import { IconBuildings } from '@tabler/icons-react';

// constant
const icons = { IconBuildings };

const schools = {
    id: 'schools-page',
    title: 'Schools',
    type: 'group',
    url: '/schools',
    icon: icons.IconBuildings,
    breadcrumbs: true
};

export default schools;
